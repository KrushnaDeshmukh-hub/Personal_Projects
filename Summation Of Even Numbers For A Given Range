
#include <iostream>
using namespace std ;

int main()
{
  int start;
  cout << "Enter Starting Point Of Range: ";
  cin >> start;
  int end;
  cout << "Enter Ending Point Of Range: ";
  cin >> end;
  
  int total = 0;
 for(int i=start ;i<=end ;i ++){
     if (i%2 == 0){
         total = total + i;
     }}
 if (end < start){
     cout << "ayyee , trying to to go backwards? sad to inform , but can't do that here";
 }
  else {cout << "The Summation Of Even Numbers In The Given Range Is: " << total ;
    return 0;}
}
